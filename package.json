{
  "name": "umbrel-android",
  "version": "1.0.0",
  "description": "Umbrel Home Server for Android",
  "scripts": {
    "test": "echo \"Running tests in packages/android/umbrel-app...\" && cd packages/android/umbrel-app && ./gradlew test"
  }
}
