{
  "name": "umbrel-android",
  "version": "1.0.0",
  "description": "Umbrel Home Server for Android",
  "private": true,
  "scripts": {
    "test": "echo \"Running tests in packages/android/umbrel-app...\" && cd packages/android/umbrel-app && gradle test",
    "build:debug": "cd packages/android/umbrel-app && gradle assembleDebug",
    "lint": "cd packages/android/umbrel-app && gradle lintDebug"
  }
}
